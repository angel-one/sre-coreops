{
  "Version": "2012-10-17",
  "Statement": [
    {% for bucket in buckets %}
    {
      "Sid": "S3AdminAccess{{ loop.index }}",
      "Effect": "Allow",
      "Action": "s3:*",
      "Resource": [
        "arn:aws:s3:::{{ bucket }}",
        "arn:aws:s3:::{{ bucket }}/*"
      ]
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
